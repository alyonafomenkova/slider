type Configuration = {
  min: number;
  max: number;
  step: number;
  from: number;
  to: number;
  isVertical: boolean;
  hasInterval: boolean;
  hasValue: boolean;
  hasScale: boolean;
};

export default Configuration;
