type Configuration = {
  min: number;
  max: number;
  step: number;
  currentValueFrom: number;
  currentValueTo: number;
  isVertical: boolean;
  hasInterval: boolean;
  hasPointer: boolean;
  hasScale: boolean;
};

export default Configuration;
